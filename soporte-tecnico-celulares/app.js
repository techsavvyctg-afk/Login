const mono=document.createElement('link');mono.rel='stylesheet';mono.href='../soporte-tecnico/theme-monochrome.css';document.head.append(mono);const stage=document.querySelector('.phone-stage'),scene=document.querySelector('.scene');if(stage&&matchMedia('(pointer:fine)').matches){scene.addEventListener('mousemove',e=>{const r=scene.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;stage.style.transform=`rotateY(${-13+x*15}deg) rotateX(${5-y*12}deg) translateZ(8px)`});scene.addEventListener('mouseleave',()=>stage.style.transform='rotateY(-13deg) rotateX(5deg)')};document.querySelector('.request-form').addEventListener('submit',e=>{e.preventDefault();const message=e.currentTarget.querySelector('.message');message.textContent='Solicitud recibida. Le contactaremos por WhatsApp para confirmar el diagnóstico.';e.currentTarget.reset()});
